利用NotebookLM AI 协助整理资料并导入 Anki 是一套考研复习策略。

为了让生成的文本能够顺畅无阻地转换为带有图片的 Anki 卡片，我们需要在 NotebookLM 端设定极其严格的输出格式，并在后期使用一个简单的脚本进行批量转换。

以下是为你梳理的自动化工作流程与执行计划：

1. **配置 NotebookLM 专属提示词:** 固定输出格式是自动化的前提.
在 NotebookLM 的聊天框中，首先发送以下提示词，将其设定为你的“专属出题机器”。这个格式使用了特定的分隔符，极其方便后续使用代码解析：


``` prompt

请严格基于我上传的文档内容进行总结和提取。
每次我输入一个考点或问题，请将其转化为“问答形式”的 Anki 卡片，并**严格按照以下 Markdown 格式输出。绝不要包含任何寒暄、过渡句或自我解释**。
**【排版与制卡规则】**
1. 注意最重要，你需要完全按照给出的资料内容进行说明，不允许自行发挥。
2. 数学与化学公式：当遇到公式、酶促反应动力学方程、或者热力学计算（如 ΔG）时，当必要时请务必使用 MathJax (LaTeX) 语法。行内公式使用 \(公式\)，独立成行的块级公式使用 \[公式\]。不要使用 $ 或 $$ 符号。
3.如果给出的问题表明需要，或者没有指出但是使用表格此时最为恰当，你可以使用markdown表格进行比较（仅限于必要时使用，如名词解释则不需要）。
4.卡片分割（重要）：你必须在每一张卡片的最后一行输出 --- 作为结束符。如果我要求你一次性生成多张卡片，请务必用 --- 严格将每一张卡片隔开。

以下是一个例子。

 ### Front
 [我输入的考点名称或生成的核心问题，eg.名词解释：xxx]

 ### Back
[针对输入问题做出的解释，eg.xxxxx（针对上述的解释）]

---
 
注意我主要会询问两种问题，分别是名词解释和论述
当我提出名词解释的时候，内容应当简洁明了。基于原文进行名词解释，名词解释时不要给出代谢途径。
当我提出论述的时候，需要基于原文论述
当出现生化流程，注意标注那些是限速酶。

 
```

2. **交互与内容积累:** 在日常复习中沉淀数据.
开始日常提问。例如输入：“解释尿素循环，特点以及意义”。
NotebookLM 会按照上述格式输出。你只需要在电脑上新建一个 `biology_anki.md` 的文本文件，将每次 NotebookLM 输出的文本直接复制粘贴进去。
文件中会有很多由 `---` 隔开的知识块，这就构成了你的卡片数据库。


3. **图片资源准备:** 建立本地媒体库.
NotebookLM 无法直接生成你教材中的高清本地图片。给 NotebookLM 的提示词应改为：`图片：如需配图，请输出 ![Markdown](media/图表名称.png)，不需要则留空。` 看到图片引用后，你需要截图或保存相关图片，使用引用中的文件名，并将所有图片统一放在 Markdown 文件旁的 `media` 文件夹中。


4. **脚本转换与打包:** 将 Markdown 转换为 Anki 支持的格式.
使用 Python 编写一个简易脚本（利用 `genanki` 库）。脚本的工作逻辑如下：

1. 读取 `biology_anki.md`，以 `---` 为界限切割出每一个卡片。
2. 将 `### Front` 下方的内容识别为卡片正面。
3. 将 `### Back` 下方的内容识别为卡片背面。
4. 使用正则表达式扫描文本中的 `![说明](media/xxx.png)`，并自动替换为 Anki 识别的 HTML 标签 `<img src="xxx.png">`。
5. 脚本自动将文本内容与 `media` 文件夹中的图片打包，生成一个 `.apkg` 文件。运行前使用 `python -m pip install -r requirements.txt` 安装 `genanki` 与 Markdown 转换库。


5. **导入 Anki 并复习:** 完成闭环.
双击生成的 `.apkg` 文件，Anki 会自动导入所有卡片和对应的图片。你可以直接开始记忆。


---









